type Habit @model @auth(rules: [{allow: owner}]) {
    id: ID!
    title: String!
    priority: String!
    category: String!
    isCompleted: Boolean!
    date: String!
    startTime: String
    endTime: String
    createdAt: String!
    completedAt: String    # ← Add this (nullable — String stores timestamp)
}
